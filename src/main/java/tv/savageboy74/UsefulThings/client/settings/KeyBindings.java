package tv.savageboy74.usefulthings.client.settings;

import net.minecraft.client.settings.KeyBinding;
import org.lwjgl.input.Keyboard;
import tv.savageboy74.usefulthings.common.util.Strings;

public class KeyBindings
{
    public static KeyBinding placeholder = new KeyBinding(Strings.Keys.PLACEHOLDER, Keyboard.KEY_NUMPAD0, Strings.Keys.CATEGORY);
    public static KeyBinding placeholder2 = new KeyBinding(Strings.Keys.PLACEHOLDER2, Keyboard.KEY_NUMPAD1, Strings.Keys.CATEGORY);
}
